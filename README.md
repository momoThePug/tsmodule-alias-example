# tsmodule-alias-example
Shwocase for the module tsmodule-alias(https://github.com/momoThePug/tsmodule-alias) a module to register typescript paths aliases in Node module loader.

## Usage
```
npm install
npm run start
```
:p be happy...